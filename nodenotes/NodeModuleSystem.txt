1. create a module

every file is a module.

To make a private variable global, use module.exports.somename = somename

2. load a module

use require function
require('./somefolder/logger'); // returns what that module exports

const logger = require('./logger')
logger.log('yoyoyo')

3. built in modules in node
	frequently used:
		file system
		http
		os
			get info from current os
		path
		process
		query strings
		stream